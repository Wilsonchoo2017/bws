#!/usr/bin/env bash
cd "$(dirname "$0")"
# Kill any existing process on port 8005
lsof -ti :8005 | xargs kill -9 2>/dev/null || true
.venv/bin/uvicorn api.main:app --reload --host 0.0.0.0 --port 8005
