"""Postgres write helpers -- DEPRECATED.

All repository code now writes directly via PgConnection (raw SQL).
The ORM dual-write functions that lived here have been removed.
This file is kept as a placeholder to avoid import errors from
any stale .pyc files; it can be deleted once confirmed safe.
"""
